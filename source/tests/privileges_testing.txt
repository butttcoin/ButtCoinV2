Add user to a root, check if root, whitelisted, blacklisted (should be root only).  -PASSED
Add user to a whitelist, check if root, whitelisted, blacklisted (should be whitelisted AND root). -PASSED
Add user to a blacklist, check if root, whitelisted, blacklisted (should be blacklisted only). -PASSED
Add user to a blacklist then root, should be root only -PASSED
Add user to blacklist then whitelist, should be whitelist only -PASSED
Add to blacklist then whitelist then root, should be whitelisted AND root -PASSED 

